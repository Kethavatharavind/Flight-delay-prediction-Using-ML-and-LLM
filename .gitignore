# Python
__pycache__/
*.py[cod]
*$py.class
*.so
venv/
.env

# IDEs
.vscode/
.idea/

# Local databases (don't push - too large)
data/*.db
data/*.csv

# JSON state files (local only in data folder)
data/rl_q_table.json
data/rl_metrics.json
data/pending_predictions.json
data/long_term_summary.json

# Config files that should be tracked (NOT ignored)
# config/ is tracked

# Jupyter
.ipynb_checkpoints/
*.ipynb

# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Keep these folders tracked (NOT ignored):
# - src/       (core code)
# - scripts/   (utility scripts)
# - tests/     (test files)
# - config/    (configuration)
# - models/    (ML models - needed for predictions)
# - templates/ (HTML)
# - static/    (CSS/JS)
